package play

//go:generate counterfeiter --fake-name UISpy . UI
type UI interface {
    Winner(string)
    Draw()
}

type GameUseCase struct {
    UI UI
}

type Inputs struct {
    Player1Name, Player2Name string
    Player1Throw, Player2Throw Throw
}

func (g GameUseCase) Play(playerThrows map[string]string) {
    g.UI.Winner("desmond")
    throws := []string{}
    for k, v 
    
}
